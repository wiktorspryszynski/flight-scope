# PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=
POSTGRES_PORT=

# Redis
REDIS_HOST=
REDIS_PORT=
REDIS_DB=

# Flight data
FLIGHT_POSITION_TTL_SECONDS=
FLIGHT_FETCH_INTERVAL_SECONDS=120 # backend fetcher loop interval; keep in sync with VITE_FLIGHT_FETCH_INTERVAL_SECONDS
VITE_FLIGHT_FETCH_INTERVAL_SECONDS=120 # baked into frontend bundle at build time; controls SSE timeout and animation duration
MAX_SNAPSHOTS=

# Frontend
FRONTEND_PORT=
VITE_API_BASE_URL= # has to include http:// or https://
VITE_MAPTILER_KEY=

# Backend
OPENSKY_LOGIN=
OPENSKY_PASSWORD=
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# Dev
DEV_DUMMY_DATA=false
COMPOSE_PROFILES=dev # prod (VPS) or dev (local)